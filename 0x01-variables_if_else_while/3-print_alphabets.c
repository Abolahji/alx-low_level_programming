#include <stdio.h>
/**
 * main - void for now
 *
 * Return: zero
 */
int main(void)
{
char alx;
for (alx = 'a' ; alx <= 'z' ; alx++)
putchar(alx);
for (alx = 'A' ; alx <= 'Z' ; alx++)
putchar(alx);
putchar('\n');
return (0);
}
