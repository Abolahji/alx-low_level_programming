#include "main.h"
/**
 * add -  a function that adds two integers and returns the result.
 * @iftar: a parameter
 * @sahur: a parameter
 * Return: is the sum of variables
 */
int add(int iftar, int sahur)
{
iftar = 48;
sahur = 50;
return (iftar + sahur);
}
